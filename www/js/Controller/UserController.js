/**
 * User Controller
 */

Reservation
	.controller('UserController', ['$scope', function($scope) {
		$scope.password = { };
		$scope.user = { };

		/**
		 * Change Password
		 */
		$scope.changePasswordSubmit = function(formValid) {

			if (formValid) {
				
			} else {
				
			}
		};
	}]);